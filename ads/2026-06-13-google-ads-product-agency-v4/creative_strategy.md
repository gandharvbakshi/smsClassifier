# Creative Strategy

This run tests the generic product-agency rule. Hooks must include the app action in the hook itself, and videos must reveal product agency plus stakes within the first 3 seconds.

Primary: `Delivery? App shows account change`.
Secondary: `Prize SMS asks KYC? App warns`.

Discarded: precaution-only hooks such as `Pause before sharing` and `Check suspicious texts first`.
