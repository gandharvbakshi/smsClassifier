v1.0.15
- Improves OTP-autofill compatibility with apps like Amazon: full SMS body and an "Autofill code" hint are now exposed in the notification so the system OTP picker can read it.
- Adds an internal diagnostic screen (debug builds only) for verifying notification structure.
