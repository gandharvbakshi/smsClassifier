v1.0.16
- Adds optional privacy/consent for analytics and crash reporting (Firebase).
- Free tier uses on-device heuristics only; full server classification unlocks after your first OTP is detected (7-day trial), then requires purchase when added in a future update.
- Same OTP notification improvements as v1.0.15.
