Optional “Help improve classification”: when enabled in Settings → Feedback, misclassification reports (“Report as wrong”) can upload SMS text and sender plus predicted labels over HTTPS so we can improve the model — off by default. Privacy policy updated accordingly.
