v1.2.11 - Inbox loading hotfix
- Fixes an inbox spinner issue on phones with very large SMS histories.
- Speeds up conversation lookup and avoids duplicate startup database work.
- Keeps Inbox on the selected filter instead of auto-switching after launch.
